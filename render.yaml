services:
  - type: web
    name: incident-analyzer-api
    runtime: node
    plan: free
    buildCommand: pnpm install && pnpm --filter @incident-analyzer/shared build && pnpm --filter @incident-analyzer/api build
    startCommand: cd apps/api && pnpm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: MONGO_URI
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: ANALYSIS_MODE
        value: rule-based
